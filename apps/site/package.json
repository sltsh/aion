{
  "name": "@sltsh/aion-site",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "prebuild": "npm run build -w @sltsh/aion-tokens && npm run build -w @sltsh/aion-css",
    "build": "vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "pretypecheck": "npm run build -w @sltsh/aion-tokens && npm run build -w @sltsh/aion-css",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@sltsh/aion-css": "*",
    "@sltsh/aion-lab": "*",
    "@sltsh/aion-tokens": "*",
    "@sltsh/site-mark": "0.1.0"
  }
}
